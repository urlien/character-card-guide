# 明日方舟剧情 工作日志与操作手册

> 本文件是**更新本目录的唯一入口**。下次出新活动时：先读本文件 → 按"操作流程"执行 → 在"工作日志"里追加一行。

---

## 📌 这是什么

`character-card-guide/明日方舟剧情/` 存放明日方舟全量剧情原文（原始指令脚本格式），按活动/主线/干员密录分目录组织。共 **2001 个章节文件**（截至 2026-08-23）。

## 🗂 目录结构

```
明日方舟剧情/
├── <活动名>/              ← 75 个活动，每个活动一个文件夹
│   ├── <章节名><关卡号> <类型>.txt  例：收信地：终点TO-1 行动前.txt
│   └── ...（每个章节独立文件）
├── 主线剧情/
│   ├── 第00章 ~ 第17章/   ← 主线按章分（含教程关TR）
│   └── ...
├── 干员密录/
│   └── <干员中文名>/      ← 332 个干员
└── 其他剧情/
    ├── 游戏引导/
    └── 肉鸽/             ← Roguelike 剧情
```

## 📏 命名规则

- 文件名 = `章节名 + storyCode + 空格 + avgTag`，如 `一封想写的信TO-ST-1 幕间.txt`
- 半角冒号 `:` 转为 `_`（Windows 兼容），如 `6:44P.M.` → `6_44P.M.`
- 全角标点（`：` `！` `，`）保留

## 🔧 数据源

| 用途 | 地址 |
|------|------|
| 剧情索引 story_review_table.json | `https://r2.m31ns.top/zh_CN/gamedata/excel/story_review_table.json`（国内直连可用） |
| 完整数据源目录树（含隐藏关/肉鸽） | `https://github.com/050644zf/ArknightsStoryJson`（树接口：`api.github.com/repos/050644zf/ArknightsStoryJson/git/trees/main?recursive=1`） |
| 剧情原文 txt | `https://raw.githubusercontent.com/Kengxxiao/ArknightsGameData/master/zh_CN/gamedata/story/<path>.txt` |
| 剧情原文 JSON（ArknightsStoryJson 格式，Kengxxiao 缺文件时用它） | `https://raw.githubusercontent.com/050644zf/ArknightsStoryJson/main/zh_CN/gamedata/story/<path>.json` |
| 干员映射（密录按干员分类用） | `character_table.json`（r2 镜像，20MB，走代理） |

> ⚠️ **网络注意**：国内直连 `raw.githubusercontent.com` 会卡死 0B/超时。必须走 Clash 代理 `http://127.0.0.1:7897` 或用 r2 镜像。aria2c 加 `--all-proxy="http://127.0.0.1:7897"`。

## 📃 操作流程（下次出新活动时）

### 第 0 步：读本文件，进入工作目录

```bash
# 判断当前情况：仓库是否已 clone 到本地
ls D:\Hermes\暂存文件\ak-rebuild\repo 2>/dev/null || echo "需要重新 clone"
```

### 第 1 步：拉最新索引，对比缺什么

```python
# 用 r2 拉最新 story_review_table.json
# 用 git trees API 拉 ArknightsStoryJson 完整目录树（真实全量基准）
# 对比：数据源全量 vs 仓库已有文件 → 得到缺失清单 download_missing.txt
```

**关键：** 完整性基准是 **ArknightsStoryJson 的 zh_CN/gamedata/story/ 全树**（当前 2001 个）。不能只信 story_review_table——它不收录隐藏关/肉鸽。对比时注意：
- 树路径带 `zh_CN/gamedata/story/` 前缀（无前导斜杠），转成相对路径 `.json` → `.txt`
- 只统计 `zh_CN/`（仓库树含 en_US/ja_JP 等多语言，全语言合计 9519 个，别混淆）

### 第 2 步：下载缺失文件

```bash
# 生成 aria2c 输入（URL + out=raw/<相对路径>）
aria2c -i aria_missing.txt --dir=D:\Hermes\暂存文件\ak-rebuild \
  --all-proxy="http://127.0.0.1:7897" \
  --max-concurrent-downloads=12 --max-connection-per-server=2 --split=2 \
  --timeout=30 --retry-wait=3 --max-tries=8 --console-log-level=warn
```

### 第 3 步：归类生成

- 活动：`raw/activities/<actXX>/...` → `out/<活动名>/<章节名+storyCode+avgTag>.txt`
- 主线：`obt/main/level_main_XX-` 或 `level_st_XX-` 或 `level_spst_XX-` → `out/主线剧情/第XX章/`
- 教程 TR：`level_main_tr` → `out/主线剧情/第00章/`
- 密录：`obt/memory/story_<干员id>_N_M` → `out/干员密录/<干员中文名>/`（干员名从 character_table.json 的 `char_*_<id>` 匹配）
- 引导：`obt/guide/` → `out/其他剧情/游戏引导/`
- 肉鸽：`obt/Roguelike/` → `out/其他剧情/肉鸽/`
- 隐藏关：`activities/.../level_*_hidden_stXX` → `out/<活动名>/隐藏关/`（storyName 从同路径 JSON 取，如 逃离/选择/路漫漫）

### 第 4 步：质量校验

- 文件总数 == 数据源全量树数
- 无空文件（<50B）
- 无 Windows 非法字符文件名（`<>:"|?*`）——半角冒号已转 `_`
- 抽查正文开头是 `[HEADER(...)]`

### 第 5 步：同步到仓库 + 推送

```bash
# 拷贝 out/ → repo/明日方舟剧情/
# git add -A && git commit -m "..." && git push <token-url> main
```

⚠️ 推送必须用 token URL：`https://<TOKEN>@github.com/urlien/character-card-guide.git`（token 由用户在开始前提供，用完即失效）
⚠️ **只准改 `明日方舟剧情/`，其他目录一律不动**（角色卡制作指南/黍/discord技术库 等）
⚠️ 删除旧文件前先把清单给用户过目

---

## 📝 工作日志

### 2026-08-23 — 全量重建（2001 章节）
- **背景**：旧结构是 554 个平铺 txt（一活动一个大合并文件），存在 91 个误提交的章节拆分文件、160B 骨架占位、10 个半角冒号文件名（Windows 无法检出）
- **做了什么**：
  1. 用户确认后弃旧重爬：从 ArknightsGameData / ArknightsStoryJson 全量下载
  2. 生成新结构：75 活动文件夹 + 主线剧情(第00-17章) + 干员密录(332干员) + 其他剧情
  3. 1993 个章节（story_review_table 覆盖）+ 补充 8 个遗漏（长夜临光隐藏关3 + 肉鸽RO1剧情5）= 2001 个
  4. 删除旧 554 txt，替换新结构
  5. 两次推送：`28fa55d`（1993 主体）+ `c760f89`（补 8 个）
- **关键教训**：
  - story_review_table 会漏隐藏关/肉鸽 → 必须用 ArknightsStoryJson 全树核对
  - 国内直连 raw.githubusercontent.com 卡死 → 必须走 Clash 代理
  - 转换 JSON→txt 时布尔值 `"true"` 去引号、属性追加空格、HEADER 加 storyName 后缀，才和 Kengxxiao txt 一致
- **状态**：✅ 完成。全量 2001/2001，远端 HEAD `c760f89`

### 下次更新流程回顾
1. 读本文件
2. r2 拉索引 + API 拉全树 → 对比出缺失
3. aria2c 代理下载
4. 归类生成（按上述规则）
5. 校验 2001+ 总数
6. 同步 repo、确认删除清单、commit、push
7. **在下面追加一行日志**